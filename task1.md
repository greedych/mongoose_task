Задание 1

Инициализация проекта, настройка подключения к базе данных MongoDB

    1. Инициализация нового проекта

Инициализируйте новый проект в удобной вам директории.
Установите необходимые зависимости: express, dotenv.
Подготовьте структуру файлов и папок.
Настройте простой Express сервер.

    2. Установка Mongoose

Запустите терминал или командную строку в корневой папке вашего проекта Express.
В терминале введите следующую команду для установки Mongoose:
npm install mongoose

    3. Подключение Mongoose к вашему проекту

Откройте файл, в котором настроен ваш Express сервер.
В верхней части файла, где вы импортируете необходимые модули, добавьте код для подключения Mongoose.

    4. Настройка подключения к MongoDB

Определите строку подключения к вашей базе данных MongoDB.

    5. Управление подключением

Настройте Mongoose для обработки подключения к базе данных. Убедитесь, что вы добавили обработку успешного подключения и возможных ошибок.

    6. Тестирование подключения

Вновь откройте терминал в корне вашего проекта и запустите Express сервер.
Если подключение к базе данных настроено правильно, вы увидите сообщение о успешном подключении к MongoDB в консоли.

    Задание 2

Создать связь "один ко многим" между моделями `Author` и `Book`

    1. Определение модели автора

Создайте новую схему `Author`, которая будет содержать поля, такие как `name` (имя автора) и `bio` (биография).
Создайте модель `Author`, используя схему `Author`.

    2. Определение модели книги

Создайте новую схему `Book`, которая будет содержать поля, такие как `title` (название книги) и `publishedDate` (дата публикации).
Добавьте поле `author`, которое будет содержать ссылку на модель `Author`.
Создайте модель `Book`, используя схему `Book`.

    3. Экспорт и использование моделей

Убедитесь, что обе модели экспортированы и доступны для использования в других частях приложения.

Задание 3

Создать связь "" между моделями `Category` и `Product`

    1. Определение модели категории

Создайте новую схему `Category`, которая будет содержать поле `name` для хранения названия категории.
Создайте модель `Category`, используя схему `Category`.

    2. Определение модели продукта

Создайте новую схему `Product`, которая будет содержать поля, такие как `name` (название продукта) и `price` (цена продукта).
Добавьте поле `category`, которое будет содержать ссылку на категорию (`Category`), к которой относится продукт.
Создайте модель `Product`, используя схему `Product`.

    3. Экспорт и использование моделей

Убедитесь, что обе модели экспортированы и доступны для использования в других частях приложения.

Задание 4

Создать связь "многие ко многим" между моделями `Student` и `Course`

    1. Определение модели студента

Создайте новую схему `Student`, которая будет содержать поля, такие как `name` (имя студента) и `email` (электронная почта).
Добавьте поле `courses`, которое будет массивом ссылок на курсы (`Course`).
Создайте модель `Student`, используя схему `Student`.

    2. Определение модели курса

Создайте новую схему `Course`, которая будет содержать поля, такие как `title` (название курса) и `description` (описание курса).
Добавьте поле `students`, которое будет массивом ссылок на студентов (`Student`).
Создайте модель `Course`, используя схему `Course`.

    3. Экспорт и использование моделей

Убедитесь, что обе модели экспортированы и доступны для использования в других частях приложения.

Задание 5

Создать связь "многие ко многим" между моделями `Actor` и `Movie`

    1. Определение модели актера

Создайте новую схему `Actor`, которая будет содержать поля, такие как `name` (имя актера) и `birthdate` (дата рождения).
Добавьте поле `movies`, которое будет массивом ссылок на фильмы (`Movie`).
Создайте модель `Actor`, используя схему `Actor`.

    2. Определение модели фильма

Создайте новую схему `Movie`, которая будет содержать поля, такие как `title` (название фильма) и `releaseYear` (год выпуска).
Добавьте поле `actors`, которое будет массивом ссылок на актеров (`Actor`).
Создайте модель `Movie`, используя схему `Movie`.

    3. Экспорт и использование моделей

Убедитесь, что обе модели экспортированы и доступны для использования в других частях приложения.
